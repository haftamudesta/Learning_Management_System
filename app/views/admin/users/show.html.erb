<div class="flex flex-col items-center w-full">
  <p style="color: green"><%= notice %></p>

  

  <div class="flex justfiy-start gap-6 mb-6 mt-6">
    <%= link_to "Back to Users",admin_users_path,class:"bg-green-400 font-semibold text-red-400 rounded-lg px-4 py-1 " %>
    <%= button_to "Delete User", [:admin,@user], method: :delete, data: {'turbo-confrim':"Are you sure you want to delete this User"},class:"bg-red-400 hover:bg-red-600 font-semibold text-white-50 rounded-lg px-4 py-1" %>
  </div>
  <div>
    <%= render @admin_user %>
  </div>
  
</div>
