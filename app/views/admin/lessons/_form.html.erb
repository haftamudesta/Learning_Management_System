<%= form_with(model: [:admin, @admin_course,admin_lesson]) do |form| %>
  <% if admin_lesson.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(admin_lesson.errors.count, "error") %> prohibited this admin_lesson from being saved:</h2>

      <ul>
        <% admin_lesson.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="flex flex-col">
     <label class="text-sm" for= "title">Title:</label>
     <%=form.text_field :title,class:"rounded", id:"titl", placeholder: "Enter Lesson Title"%>
  </div>
  <div class="flex flex-col">
    <label class="text-sm" for= "description">Description:</label>
    <%=form.text_field :description,class: "rounded", id: "description", placeholder: "Enter Lesson Description"%>
  </div>
  <div class="flex flex-col">
    <label class="text-sm" for= "paid">Paid:</label>
    <%=form.check_box :paid,class: "rounded", id: "paid" %>
  </div>
  <div class="flex flex-col">
    <label class="text-sm" for= "paid">Paid:</label>
    <%= admin_lesson.video.present ? (video_tage admin_lesson.video, class:"w-64 rounded"): nil %>
    <%=form.file_field :video,class: "rounded", id: "video" %>
  </div>
  <div>
    <%= form.submit "Submit",class: "bg-puple-400 text-white-100 px-4 py-2 rounded-lg cursor-pointer" %>
  </div>
<% end %>
